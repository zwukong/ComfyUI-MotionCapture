# ComfyUI-MotionCapture Dependencies
# Compatible with ComfyUI's existing PyTorch installation

# SKIPPED: PyTorch/torchvision (use ComfyUI's existing installation)
# timm - Vision transformer library
timm>=0.9.0

# Training/inference frameworks
lightning>=2.0.0
hydra-core>=1.3
hydra-zen
hydra_colorlog

# Core utilities (flexible versions for ComfyUI compatibility)
numpy>=1.23.0,<2.0.0
einops
rich
termcolor
joblib

# Computer vision
opencv-python
scikit-image
imageio>=2.34.0
av>=13.0.0

# Video processing
ffmpeg-python

# Visualization (matplotlib and tensorboardX removed - not needed for inference)

# 3D vision and body models
# SKIPPED: pytorch3d (replaced with custom pure-PyTorch shim in vendor/hmr4d/utils/pytorch3d_shim.py)
trimesh
chumpy-fork
smplx
# wis3d removed - only used for training visualization
pycolmap

# BVH (Biovision Hierarchy) file format support
bvh>=0.3

# 2D pose estimation (ViTPose dependencies)
# Note: ultralytics (YOLO) is NOT needed as we use SAM3 for segmentation
cython_bbox
lapx

# Additional utilities
setuptools>=68.0
yacs

# Model downloads
huggingface_hub>=0.20.0
gdown>=4.7.0
